// Placeholder: agent module
// src/agent/agent.ts
//
// GPT-4o agent loop with function calling.
// This file orchestrates the conversation: receives user message,
// appends to history, calls GPT-4o, executes any tool calls,
// feeds results back, and streams the final response via SSE.
//
// TODO (Phase 2): Implement agent loop

export {};
